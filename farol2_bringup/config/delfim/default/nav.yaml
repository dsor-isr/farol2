################################
#  Parameters
################################
/**:
  filter_node:
    ros__parameters:
      node_frequency: 10.0
      publish_all_steps: true

      filters: 
        - "" 
      measurements:
        - "gnss"
        - "imu"
        - "altimeter"
        - "thruster_rpm"
      
      plugins:
        sample_and_hold:
          initializer_measurements:
            - "gnss"
            - "imu"
